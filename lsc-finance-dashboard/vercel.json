{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm exec -- pnpm install --no-frozen-lockfile",
  "buildCommand": "cd apps/web && npm exec -- next build; if [ -d .next ]; then cd ../.. && rm -rf .next && cp -R apps/web/.next .next; else exit 1; fi",
  "devCommand": "npm exec -- pnpm --filter web dev"
}
