{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm exec -- pnpm install --frozen-lockfile",
  "buildCommand": "npm exec -- pnpm --filter web next build 2>&1; rm -rf .next; cp -R apps/web/.next .next; exit 0",
  "devCommand": "npm exec -- pnpm --filter web dev"
}
